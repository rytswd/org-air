# Emacs byte-compiled output
*.elc

# Test dependencies (installed by the Makefile via package.el)
/.deps/

# direnv local cache (the flake devshell)
/.direnv/

# nix build result symlinks
result
result-*

# README screenshots — hosted on GitHub's user-content CDN, not committed
# (keeps clones lean). Local staging dir for captures before upload.
/docs/images/
