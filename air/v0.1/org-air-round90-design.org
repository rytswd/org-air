#+title: org-air Round 90 — local backlog focus, first-class marked backlog batches, and a collapsed-by-default Backlog lens
#+state: work-in-progress
#+tags: board triage backlog marks bulk undo focus collapse ux robustness

* Summary

This round closes three connected dashboard interaction gaps without adding a
query path:

1. =b= must stop following the item it just moved.  After a row leaves its
   current section, point stays local: the successor that slides into the
   same section row wins; on the old section's last row, the nearest previous
   row wins.  The moved item's new Backlog/non-Backlog home is never the
   fallback.
2. =m= becomes a real selection model.  Today it stores only an
   =org-air-marked= text property on one rendered line and replaces that
   line's first byte with =•=.  No command reads the property; the next
   repaint erases it.  R90 stores marks by durable source identity, renders
   them from state, shows their count, and makes =b= apply once to the whole
   marked set.  The existing add-tag command =t= is the one shared-prompt
   companion included in this round.  Riskier/semantically ambiguous actions
   are explicitly blocked while marks are active rather than silently acting
   on point.
3. Backlog stays on the normal dashboard as a compact header/count but is
   header-only by default.  =TAB= reveals it.  Session/bookmark expansion
   survives ordinary repaints and refreshes; an explicit =is:backlog= lens
   reveals the rows because asking for that lens is an explicit request to
   see them.

The implementation remains a cached-data mutation followed by one repaint.
There is no org-ql scan on mark, bulk action, filter, collapse, or repaint.
Source writes remain Org-native and use the R68 logging/atomic discipline.
One marked command creates one user-facing compound undo entry, including
when marked items span files.  Disk saves cannot be made transactionally
atomic across files with the current machinery; the exact per-file commit
and failure law is specified below instead of claiming otherwise.

Implemented on 2026-07-25.  The decisions below remain the normative
contract; implementation and gate details are recorded in Implementation
History.

* Investigation: the three current failure shapes

** Why `b' follows the moved item

R83's =org-air-item-backlog= writes the tag, mutates the cached item's =tags=
slot, removes that item's classify memo entry, then calls
=org-air-view--refresh-current=.  The generic point token stores the row's
=org-air-marker=.  =org-air-view--restore-position= prefers that exact marker
wherever it renders after the repaint.  The item still exists, now under
Backlog (or back in a task section), so the generic identity-preserving rule
faithfully jumps to the moved row.  That rule is right for sort/resize and
wrong for a triage move.

The fix is not to weaken generic point preservation.  Mutation commands need
an explicit local landing plan whose exclusions say “do not follow these
moved source keys”.

** What `m' stores today, and why it cannot drive work

=org-air-toggle-mark= has no selection collection.  It:

- reads =org-air-marked= from point,
- writes the inverse property only over the current rendered line, and
- deletes/reinserts the first character as a space or =•=.

Consequences:

- no action enumerates marked items;
- a full render, section splice, filter, sort, refresh, day transition, or
  resize destroys the property;
- duplicate rows for one source heading can disagree visually;
- there is no stable identity, count, stale-mark handling, clear-all command,
  undo description, or bulk error model;
- replacing byte 1 assumes the line's first column is expendable instead of
  rendering through the row primitive.

The README's “Toggle mark” and the original design's “mark/unmark item for
bulk action” therefore promise more than the implementation supplies.

** Why Backlog shows rows on a cold board

R83 appended a normal =backlog= descriptor.  Only =notes= has special
header-only logic in =org-air-view--displayed-for-bucket-1= and fold-row
logic in =org-air-view--insert-section=.  Backlog follows the ordinary
section cap, so a nil =org-air-view--expanded-sections= still renders up to
=org-air-section-max= rows.  The expansion list already survives repaint;
the missing piece is defining “not expanded” as zero Backlog rows.

* Goals and non-goals

** Goals

- Preserve rapid keyboard triage: row, =b=, next local row, =b=.
- Make marks identity-backed, repaint-stable, deduplicated, visible, countable,
  clearable, and actionable.
- Ship a useful batch now: marked =b= and marked add-tag =t=.
- Use one source write transaction per touched file, one cached repaint per
  command, and one compound recent-edit entry per marked command.
- Be explicit about partial cross-file failure, stale/ineligible marks, and
  compound undo/redo limits.
- Make Backlog compact by default but easy to discover and recover.
- Preserve all no-mark command behavior.

** Non-goals for R90

- No generic “run every item command on marks” loop.
- No bulk refile/archive/kill, no repeated note text, and no multi-field bulk
  transient.
- No fuzzy title fallback for marks after a real rescan.  Exact source
  identity wins over guessing between duplicate titles.
- No mark persistence in the disk scan cache or across a killed/new board
  buffer.
- No removal of Backlog as the shipped default.
- No org-agenda and no dependency beyond Org/org-ql; Emacs remains 29.1+.

* Decision 1 — keep option A: compact Backlog header, collapsed by default

The alternatives are:

| Option | Benefits | Costs / ruling |
|--------+----------+----------------|
| A. Keep =Backlog N=, header-only by default, =TAB= reveals | The defer action has an immediately discoverable destination and count; un-backlog is keyboard-reachable; no permanent row wall | One compact section header and one conditional Summary row when non-empty. *Chosen.* |
| B. Remove it from the normal dashboard, expose only through =is:backlog= | Zero ordinary board lines | A user can press =b= and see the item vanish with no visible destination; recovery requires remembering filter grammar; worse discoverability and reversibility. Rejected as the default. |

The existing =org-air-show-backlog-section= remains the opt-out for users who
want B.  R90 makes that opt-out honest: when nil, both the ordinary Backlog
section and its ordinary Summary row are absent.  An explicit
=is:backlog= token overrides the display opt-out for that lens only; an
explicit request must never produce an item count with no item section.

** Exact collapsed/expanded contract

- Cold new board buffer: =backlog= is not in
  =org-air-view--expanded-sections=, so a non-empty Backlog section renders
  exactly its header/count and zero item/fold rows.
- =TAB= on the Backlog header adds =backlog= to the existing expansion list
  and shows all currently visible backlog rows in the active sort order.
  A second =TAB= removes it and returns to the one header.  Explicit expansion
  is allowed to be large because it is user-requested; paging is a deferral.
- Ordinary cached repaint, resize, column toggle, sort, source scope, section
  splice, =g r= refresh, =g R= refresh-all, and the R53 progressive/final
  paints do not reset the expansion list.
- A bookmark/activity record already saves =org-air-expanded=.  A manually
  expanded Backlog therefore reopens expanded from that record; an absent or
  old field means the new collapsed default.  No second persistence store is
  added.
- Burying and reopening the same live board buffer keeps its buffer-local
  state.  Killing it and opening a new buffer is a cold open and collapses.
- =#backlog= is raw tag membership and does not auto-expand.  Any parsed
  exact =is:backlog= token (alone or in a multi-term AND/OR expression)
  adds =backlog= to the saved expansion list before the cached repaint.  This
  is stateful, not a hidden temporary override: after clearing the filter the
  user can still see where the lens went, and one =TAB= collapses it again.
  Bookmark application enforces the same rule when restoring an
  =is:backlog= filter.

Implementation seam: treat =backlog= like =notes= only for its collapsed
zero-row/fold suppression; unlike Notes, expanded Backlog is not preview-
capped.  Centralise expansion reads in
=org-air-view--section-expanded-p= so meta-width calculation, section body,
fold-row emission, TAB, and the explicit lens cannot disagree.

* Decision 2 — first-class marks use source-key identity, never row text

** The source key

Add one shared pure helper, =org-air-view--item-source-key=:

#+begin_example
(expanded-absolute-file . heading-position)
#+end_example

The position comes from the durable R53 marker slot: integer cdr for
=(FILE . POS)=, or =marker-position= for a live marker.  Headingless
=file= items may produce a key but are action-ineligible.  No title enters
the identity: two same-title headings in one file remain distinct.  No
=file-truename= is paid on motion/repaint; scan items already carry the
configured absolute source file, and =expand-file-name= is enough to
normalise a live-capture edge.

This is the same source identity already carried by =org-air-marker=,
bookmarks, review mirror members, pane contexts, and cache records; R90 gives
it one named helper instead of inventing a parallel identity.

** Mark state and rendering

- New buffer-local =org-air-view--marked-keys=: an insertion-ordered list plus
  an =equal= membership table (or one small ordered-set abstraction), keyed
  by source key.  Count means unique source headings, not rendered rows.
- =m= toggles the source key of the item at point.  It does not move point
  (preserves existing muscle behavior), writes no source bytes, creates no
  edit-ring entry, and triggers one cached repaint.
- The item row primitive derives the mark glyph from the set.  It replaces
  one existing indentation space with =•= and keeps exactly the same prefix
  width, title edge, metadata fence, and V6 alignment.  The row receives an
  =org-air-marked= property as rendered output, but that property is not the
  source of truth.
- Every duplicate board row for the same source key renders marked together
  and a bulk command targets it once.
- New installer-owned =M= clears all marks and repaints once.  It is free on
  the board map and is the mnemonic shift-pair of =m=.  With no marks it is a
  gentle “No marked items” no-op.
- Marks are board-buffer state.  They survive ordinary repaint, sort, filter,
  scope, collapse, day/board switches, resize, and bury/reopen of the same
  live buffer.  They are deliberately absent from the persistent scan cache
  and bookmark record: restoring an old selection into a destructive future
  session would be unsafe.

** Hidden, stale, and duplicate selections

- Filtering, sorting, scoping, or collapsing does not clear marks.  Hidden
  marks remain action targets.  Status therefore reports both totals, e.g.
  =3 marked · 1 shown=.  “Shown” is unique marked source keys represented by
  a rendered item row, not duplicate row count.
- On a real item-generation swap, reconcile the set by exact source key
  against the new cached items.  Exact survivors stay marked.  Missing keys
  are stale and are removed with one bounded message/status count.  There is
  no file read and no title fallback.
- At bulk-command preflight, a key absent from the current cached index is
  stale and pruned.  A present item that the action cannot accept is
  ineligible and remains marked so the user can inspect it or press =M=.
- A source file/position/title mismatch discovered when opening a file for
  the write is a failed/stale target, never silently relocated.  The message
  says to =g r=; the mark remains.

* Decision 3 — marks change command dispatch explicitly

The dispatch law is simple and visible:

1. No marks: every command behaves exactly as on R89.
2. Marks + an R90 bulk-capable command: the command targets the unique marked
   source set, including currently hidden marks; point is not implicitly
   added.
3. Marks + a single-item-only mutation: signal a soft =user-error= naming
   the command and =M= (“Refiling is single-item while 3 marks are active;
   M clears marks”).  Never mutate just point behind the selection's back.
4. Navigation, pane/visit, filter/sort/collapse, =u=/=U=, and =M= keep their
   own meanings; they are not item mutations and do not consume marks.
5. =I= (the guided Inbox walk) is blocked while marks exist.  It is a
   single-current-item loop with its own prompts; mixing it with a hidden
   marked set would be surprising.

** Complete item-action inventory and R90 ruling

| Surface / command | Class | Marks-active behavior in R90 | Rationale / natural eventual UX |
|-------------------+-------+------------------------------+---------------------------------|
| =b= backlog | Safe reversible tag metadata, no prompt | *Bulk now*: set-all toggle over every eligible mark | The required rapid defer workflow; cached =tags= is sufficient for exact no-scan repaint. |
| =t= add tag | Safe reversible tag metadata, one shared value | *Bulk now*: read one tag once, add it to every eligible mark | Existing unmarked add-only semantics stay unchanged; no pairwise toggle ambiguity. |
| =T= TODO state | Shared-value lifecycle metadata | Block this round | Natural UX is one keyword from the intersection of every target file's accepted vocabulary, require-match, then apply to all.  Current per-file vocab + =donep=/=closed=/log cache updates need a dedicated slot-sync design; partial free-typed values are unacceptable. |
| =d= deadline; M-x/=I= schedule | Shared quick-date metadata | Block this round | Natural UX is one quick-date/clear value for all.  Correct no-scan updates must cover scheduled/deadline, activity, active timestamp and day/calendar slots together. |
| priority (inside =e=) | Shared-value metadata | Block this round | Natural UX is one priority/clear value from the intersection of target-file priority ranges.  It must never cycle each item from a different starting value. |
| tags/category/schedule/deadline/TODO/priority in =e= | Mixed metadata editor | Block; =e= remains single-item after =M= | Eventual marked =e= is a metadata-only form with shared values and tag add/remove deltas.  Destination and note fields are omitted.  Reusing the current one-item prefilled replacement form would destroy mixed values. |
| dated note in =e= | Content, not metadata | Single-item only | Duplicating one prose note over many unrelated headings is not a natural default. |
| =D= done | Graduating lifecycle action | Single-item only this round | Eventual bulk DONE requires one explicit “Mark N DONE?” confirmation plus the TODO/cache work above.  A naked shifted key must not graduate a hidden set yet. |
| =e= destination / =f= / refile engine | Structural cross-file move | Single-item only | One shared destination is plausible, but inverse refile, level changes, target creation and partial disk saves are not honestly undoable today. |
| =a= archive | Structural move | Single-item only | R73 classifies it structural; source-side undo would duplicate the archived copy. |
| =x= kill | Destructive content deletion | Single-item only | Although one-buffer undo can restore one cut, a hidden multi-delete deserves a separately designed aggregate confirmation/review. |
| =u=/=U= | Global edit history | Unchanged; acts on the top edit record, not marks | A successful bulk command contributes one compound top record.  Undo/redo never restore UI marks. |
| RET/S-RET/SPC/v/V, filter/sort/scope/TAB | Navigation/view | Current-point/view semantics unchanged | Marks persist; these are not writes. |

This is intentionally narrower than “bulk-enable everything.”  R90 ships two
coherent tag operations (one no-prompt, one shared-prompt) and the complete
selection/transaction/history substrate.  The attractive shared-value
metadata form is recorded under Deferrals rather than being smuggled into the
round without cache or undo honesty.

* Decision 4 — exact marked `b' and `t' semantics

** Target eligibility

For marked =b=, eligible means:

- an =org-air-item= still present in the current cached generation,
- =kind= =heading=,
- board-active and task-routed under the same R83/R77 predicates, and
- source file/heading preflight succeeds.

A marked headingless file-item, done/archived carrier, or demoted knowledge
note is ineligible and remains marked.  This stricter bulk eligibility does
not change R83's permissive unmarked/day-row behavior.

For marked =t=, any live =kind= =heading= item is eligible; headingless
file-items are not.  One =read-string= happens before any source mutation.
Empty input is the existing add-tag behavior (Org decides/soft-errors); no new
remove semantics are invented.

** Set-all backlog toggle (mixed states)

Pairwise inversion is rejected: a mixed set would merely swap which items are
backlogged.  The set has one desired result:

- if every eligible marked item already carries =org-air-backlog-tag=,
  desired state is OFF for all;
- otherwise desired state is ON for all.

Items already in the desired state are successful no-ops: no source write for
that item, but their marks clear with the successful command.  Echo text is
precise, for example:

#+begin_example
Backlogged 5 marked items (2 already backlog); 1 ineligible remains marked
Un-backlogged 4 marked items
#+end_example

Marked =t= similarly adds the one shared tag only where absent and reports
already-tagged no-ops.

** Selection clearing

- Successful eligible targets, including desired-state no-ops, are unmarked.
- Stale keys are pruned.
- Failed, ineligible, and unattempted targets remain marked.
- A cancelled shared prompt or confirmation changes neither source nor marks.
- Undo/redo changes source data only; it does not resurrect or clear marks.

This gives a retry path after partial failure without silently re-running
already committed files.

* Decision 5 — source write coordinator, one repaint, no rescan

The existing single-item macro must not be called N times: that would save,
push history, restore point, resync panes, and repaint N times.  Add one board-
side marked-tag coordinator used only by marked =b=/=t=.

** Preflight and identity relocation

1. Snapshot the rendered focus plan (Decision 7), the ordered marked keys,
   and the current key→item index.  Resolve stale/ineligible entries before
   any write.
2. Run the existing mid-refresh stale-file guard for every candidate.  Open
   only the distinct source files that may be written; this is the same
   user-initiated source access as a single R68 write, not an org-ql scan.
3. Verify each exact position still begins the expected heading.  Build live
   temporary markers before changing bytes.
4. For every cached heading in a touched file, create a relocation marker
   from its cached source position.  Tag text can change the byte positions
   of later headings in the same file; after a successful file commit, write
   those marker positions back to the cached items' =(FILE . POS)= slots and
   rebuild surviving marked keys.  This prevents a second no-scan command
   from using stale row identities.  It is an O(items in touched files)
   slot walk, not a source/query scan.

** Per-file commit

For each source file, in deterministic file order:

- one leading =undo-boundary=;
- one =atomic-change-group= containing every changed heading in descending
  source-position order;
- R68's dynamic logging bindings around the operation, and a pending-log
  flush after each heading before another heading can overwrite Org's shared
  log globals;
- one =save-buffer=;
- only after the save succeeds, mirror exact tag values into each cached
  item's =tags= slot, remove each changed/no-op item from the classify memo,
  relocate all cached source positions for that file, and record one history
  part for the file.

A signal inside a file rolls back that whole file and records no successful
part for it.  Stop after the first failed file; later files are unattempted
and remain marked.  Earlier committed files remain committed.

After the command (full success, partial success, or no-op): rebuild the
render partition through exactly one cached repaint, consume the explicit
landing plan before inspector setup, then perform the direct pane resync.
Spies on =org-air-query-items=, =org-air-query-items-in-files=,
=org-ql-select=, refresh timers, and =org-air-refresh= must all read zero.

** Why cross-file all-or-nothing is not promised

Org change groups and undo lists are buffer-local, and =save-buffer= commits
one file at a time.  There is no current multi-file atomic rename protocol,
and rolling an already-saved file back after a later save failure can clobber
an external edit or itself fail.  Therefore the strongest honest R90 law is:

- preflight the whole target set first;
- all-or-nothing within each source file;
- deterministic stop-on-first-file-failure across files;
- exact successful/failed/unattempted counts and filenames;
- one compound history record containing only files that actually committed.

That is safer than continuing blindly, pretending rollback succeeded, or
weakening R68's per-file atomicity.

* Decision 6 — one marked command is one compound undo/redo entry

Extend the R73/R75 record shape with =:kind 'bulk= and =:parts=.  Each part is
one committed source buffer/file, its post-save chars tick, and the one
per-file undo group created above.  The top description names operation,
direction/value, and unique item count.  No item text/diff or markers are
stored.

- Zero actual writes (all targets already desired) creates no edit record.
- One marked item creates one bulk entry with one part.  One =u= reverts it
  and one =U= reapplies it.
- N marked items in one file still create one part/one undo step.
- N marked items across files create one top compound entry, not N entries.
- A fresh later edit clears compound redo through the existing R75 choke
  point.
- Structural records can never be parts; R90's coordinator accepts only tag
  metadata.

** Compound `u'/`U' law

Before touching any part, check every buffer is live, every chars tick matches,
and every expected undo/redo step is present.  A failed preflight consumes the
compound record with zero file changes and one message naming every blocker,
matching R73's stale/dead “do not eat an unrelated edit” honesty.

When preflight passes, =u= applies one =undo-only= per part in reverse commit
order and saves; =U= applies one =undo-redo= per part in commit order and
saves.  On complete success the whole record moves to the other ring and all
parts are two-sided-restamped.

The same disk limitation applies during cross-file undo/redo.  An unexpected
mid-operation save/undo failure is never called success: stop, clear the redo
branch, report “Undo incomplete: X/Y files reverted” with filenames, and
requeue only untouched remaining parts as a residual bulk undo entry.  Already
reverted parts are deliberately not advertised as redoable after this
exceptional split.  This loses convenience rather than lying about bytes or
creating a branch the linear R75 ring cannot represent.

* Decision 7 — mutation-specific local focus, never the moved source key

** Landing plan captured before the write

A landing plan contains:

- current source key (if point is on an item), section bucket, title column;
- the current section's rendered unique item-key sequence and current index;
- the full rendered unique item-key sequence as a final outward fallback;
- an exclusion set: every source key whose successful backlog state change
  can move it out of the current section.

Rendered means the rows actually on screen after filter/scope/sort/collapse,
not all cached items.  Duplicate rows are distinct section occurrences for
local ordering but resolve to one source key for exclusion.

** Landing algorithm after repaint

1. If point's item was not moved (for example point is unmarked while a
   hidden marked set changes, or a mixed set leaves the current already-
   backlog item in place), restore that exact source key and title column.
2. If it moved/disappeared, remove excluded keys from the pre-command current-
   section sequence.  Pick the survivor at the old section-local visible row
   index — the successor that slid into that row.
3. If the old index is now past the section end, pick the last survivor — the
   nearest previous item.  Thus toggling the old section's last row moves up,
   not down to Backlog or onward to another section.
4. If the section has no survivor, choose the nearest non-excluded survivor
   in the pre-command full visible order, preferring the following item then
   the previous item at equal distance.
5. Resolve by source key in the new buffer and land on
   =org-air-row-title=.  Never fall back to an excluded/moved key, even if it
   now renders in an expanded Backlog section or explicit lens.
6. If no non-excluded item remains, land on the old surviving section header,
   else the nearest section header/first visible chrome.  The bottom pane
   closes when the board is truly item-empty; the inspector shows its honest
   nil state.

This supersedes only mutation landing.  Generic
=org-air-view--restore-position= retains its same-item behavior for resize,
sort, refresh, and other repaints.

The pending mutation landing is consumed inside =org-air-view--render= after
its default/bookmark landing and *before* =org-air-view--setup-inspector= and
side-rail setup.  The final direct R73 resync then updates the follow pane,
with no timer.  Therefore board point, inline/popped inspector, bottom pane,
Summary/Actions rail, and mark count all describe one final identity; there is
no intermediate stale row observable after the command.

* Decision 8 — discoverability: status, rail, help, and messages

All additions are conditional when the mark set is empty, preserving default
board bytes.

- Banner/status: a non-sheddable-before-filter segment =• N marked=; when
  hidden marks exist, =• N marked · V shown=.  Scan progress still sheds
  decoration before progress as R56 requires.
- Rail Actions: while marks exist, a conditional bulk row shows the live keys
  derived through =org-air-view--legend-key=: =b backlog all=,
  =t tag all=, =M clear marks=.  The Actions header/suffix carries the count.
- =?= help: static Triage copy becomes “mark for bulk (b backlog, t add tag)”
  plus a =M= clear row.  When marks exist, a “Marked items” block states the
  total/shown count, that hidden marks are included, the two available bulk
  actions, and that other mutations require =M= first.
- =m= echo: =Marked N items — b backlog, t add tag, M clears= (or the unmark
  count).  Bulk completion/failure messages use the precise Decision 4/5
  counts; no per-item echo spam.
- README: the key table gains =M= clear, the =m= row names =b=/=t= bulk, and
  the Backlog paragraph says header-only until =TAB= or =is:backlog=.

The review view keeps its existing meaning of =m= (cycle review range) and its
single-item =b=.  R90's marked state/key/help belongs only to the board/day
owner buffer; no review key collision is introduced.

* Cache, source-write, and compatibility invariants

- *R53*: no org-ql scan or refresh machine on =m=/=M=/bulk =b=/bulk =t=,
  filter, collapse, or repaint.  All render decisions fold cached slots.
- *R68*: source mutations run in undisplayed source buffers under the logging
  downgrade/flush discipline; each file is one atomic group and one save.
- *R73/R75*: no fake structural inverse; one command is one bulk history
  record, redo is only fed by a complete successful undo, and fresh edits
  clear redo.
- *R83*: backlog remains an org-native tag, single-home bucket,
  =is:backlog= exact twin, day/calendar reachability, classify memo input,
  and no-scan cached mutation.  Only default body visibility and landing
  change.
- *R18/R21/R29*: generic section/render and title-landing primitives are
  reused; V6 prefix width and point normalization remain exact.
- *Filtering/sort/collapse*: marks are source selections, not row selections;
  changing a lens/order does not change targets.  Focus candidates are
  captured from the actual sorted/filtered section before mutation.
- *Source identity*: touched-file relocation updates cached =(FILE . POS)=
  slots and surviving mark keys before repaint.  No stale row property is
  retained from the old render.
- *Cache schema*: unchanged.  Marks and collapse state are UI state, and no
  =org-air-item= slot is added; no cache version bump.

* Acceptance matrix

| Case | Required result |
|------+-----------------|
| Single unmarked =b=, middle row | Source/cache tag flips; zero query calls; old section's next row occupies focus; moved key is excluded; pane/inspector/rail sync once to successor. |
| Single unmarked =b=, last row | Focus lands on nearest previous row in that section; never the new Backlog row. |
| Un-backlog from expanded Backlog or =is:backlog= | Row leaves Backlog/filter; same local successor/previous law; it never follows the item into Upcoming/attention/high-priority. |
| =m= several then =b= | Unique marked eligible headings receive one set-all operation, one repaint, one compound ring entry; successes/no-ops clear, failures remain. |
| Mixed backlog/non-backlog marks | Desired state is ON for all; already-backlogged targets are no-op successes; no pairwise swap.  A second all-backlog marked command chooses OFF for all. |
| Stale mark | Exact-key miss is pruned and reported; no file lookup fallback, no source write for it. |
| Ineligible mark | Remains marked and reported; eligible peers can commit.  If none eligible, zero write/repaint-history churn. |
| Marked =t= | One prompt, one tag value, add to all eligible marks, one repaint/compound entry; existing unmarked =t= unchanged. |
| Unsupported mutation with marks (=T/d/e/f/a/D/x/I=) | Soft error before any prompt/write, naming count and =M=; no accidental point-only action. |
| One marked item undo/redo | One bulk record/one file part; one =u= restores, one =U= reapplies; marks do not return. |
| Multi-item same-file undo/redo | One bulk record and one per-file undo group; all changed headings round-trip in one =u=/=U=. |
| Multi-file undo/redo | One compound entry; all healthy parts round-trip.  Preflight failure changes zero files; runtime partial failure reports exact split and never claims full success. |
| Cold open with backlog | =Backlog N= header and Summary count, zero Backlog item/fold rows. |
| Manual Backlog expansion + repaint/refresh | Rows remain expanded across sort, resize, column toggle, =g r=/=g R= and no-change/full swaps; bookmark round-trip preserves it. |
| Explicit =is:backlog= | Backlog descriptor is forced even if show knob nil; rows auto-reveal without scan; =TAB= can collapse afterward. |
| =#backlog= | Raw-tag filter does not auto-expand; normal saved collapse state applies. |
| Filter/sort with marks | Marks persist; hidden marks count/act; shown count is honest; focus fallback follows the pre-action sorted/filtered local order. |
| Section collapse with marks | Hidden marked rows remain targets; all duplicate occurrences share one mark; =M= clears all. |
| Pane/inspector/rail after disappearing row | Final point is chosen before inspector/rail setup; follow pane directly resyncs or closes on empty; no stale moved identity and no timer/query. |
| Touched same-file positions shift | Relocation markers rewrite every cached source key in that file; a second no-scan action reaches the correct later heading. |

* Test plan and seams

Add =tests/org-air-round90-test.el=; all tests are batch/TTY and use the real
board renderer plus source temp files.  No GUI wait is required.

1. *r90-1 current root-cause pin*: current R89 =b= follows the exact marker
   into Backlog; R90 middle-row test asserts successor title/source key and
   explicitly asserts point's key differs from moved key.
2. *r90-2 last-row + unbacklog*: last row chooses previous; expanded Backlog
   and =is:backlog= un-toggle choose local neighbors, never destination.
3. *r90-3 first-class marks*: =m= fills the ordered source-key set, all
   duplicate rows render =•=/=org-air-marked=, and a cached repaint/sort/
   filter/collapse preserves state.  =M= clears every occurrence.
4. *r90-4 marked backlog set-all*: three marks (plain, plain, already backlog)
   yield all backlog, one already-no-op count, source co-tags preserved,
   marks cleared, one repaint.
5. *r90-5 all-on → all-off*: remark that set and =b= removes only the backlog
   tag from all; other tags survive.
6. *r90-6 hidden marks*: filter/collapse hides two of three; banner/rail says
   =3 marked · 1 shown= and =b= still targets all three exactly once.
7. *r90-7 stale/ineligible*: missing key is pruned; headingless/done/demoted
   item remains marked; eligible peer commits; message counts all classes.
8. *r90-8 marked add-tag*: =read-string= spy fires once; one value lands on
   every eligible heading; no-op already-tagged item reported; one compound
   entry.
9. *r90-9 unsupported action guard*: =T/d/e/f/a/D/x/I= spies prove zero prompt,
   source write, engine, or refresh while marks exist; =M= restores their
   ordinary current-item path.
10. *r90-10 no scan / one repaint*: counters around =org-air-query-items=,
    =org-air-query-items-in-files=, =org-ql-select=, =org-air-refresh=,
    =org-air-view--render= and timer constructors: 0/0/0/0/1/0 for a marked
    command.
11. *r90-11 per-file atomicity and partial commit*: injected failure on item 2
    of one file rolls that whole file back; a later-file failure leaves the
    earlier committed file, stops before subsequent files, and leaves failed/
    unattempted marks with exact report.
12. *r90-12 source-key relocation*: two marked headings plus an unmarked later
    heading in one file; tag growth shifts bytes, cached locators update, and
    a second action at the later rendered row edits the correct source heading.
13. *r90-13 compound one/many*: one mark, many same-file marks, and cross-file
    marks each produce exactly one top record; part counts are 1/1/N-files.
14. *r90-14 compound u/U*: source bytes round-trip in all three shapes; pane
    resync spies fire from the one final repaint; a fresh edit clears redo.
15. *r90-15 compound guards/failure*: dead/tick-stale part preflight moves zero
    bytes; injected runtime part failure emits incomplete counts, no false redo,
    residual untouched parts remain honestly undoable.
16. *r90-16 cold collapse*: static non-empty backlog board renders header and
    Summary count but no titles/fold row; =TAB= reveals titles; second =TAB=
    hides them.
17. *r90-17 persistence*: no-change, sync-changed and paced-finish repaint
    paths preserve expansion; bookmark save/apply round-trips expanded and
    absent/default-collapsed states.
18. *r90-18 explicit lens/knob*: =is:backlog= forces descriptor and expansion
    with show knob both t/nil; =#backlog= does not; zero query calls.
19. *r90-19 final resync*: with pane/inspector/side rail spies, mutation landing
    is consumed before setup, each surface ends on the same successor; final
    item removal closes pane/nils inspector.
20. *r90-20 key/help/status*: board =m=/=M=/=b=/=t= bindings are installer-
    owned and strippable; review =m= remains range-cycle; mark-empty output is
    unchanged, mark-active help/rail/banner teach count, hidden inclusion,
    bulk keys, and =M=.

** Existing tests that deliberately flip

R83's semantics remain, but tests that depended on visible default Backlog
rows must be revised rather than weakened:

- r83-2 and r83-15 must expand the Backlog header or apply =is:backlog= before
  locating the row for the second =b=;
- r83-4 must assert titles absent while collapsed, then =TAB= and assert both
  titles present;
- r83-7/8/9/16 keep their no-scan/cache/ring/source-byte assertions;
- every R73/R75 ring test remains green; R90 adds =bulk= dispatch without
  changing normal =in-place=/structural behavior.

** Revert-RED and mutation plan

- Whole R90 suite against R89: mark-state/bulk/compound symbols are absent;
  current =b= follows the marker; Backlog titles render cold.  The three
  primary packages are independently RED.
- Mutation A: remove moved-key exclusion/use generic restore → r90-1/2/19 RED.
- Mutation B: store marks only as line properties → r90-3/6 RED after repaint.
- Mutation C: pairwise invert mixed backlog states → r90-4 RED.
- Mutation D: call the single-item macro per target → r90-10/13 RED (N saves,
  N records/repaints).
- Mutation E: omit touched-file relocation → r90-12 edits the wrong/lost later
  heading.
- Mutation F: continue after a failed file or clear failed marks → r90-7/11
  RED.
- Mutation G: split a command into per-item history records → r90-13/14 RED.
- Mutation H: one-sided/best-effort compound preflight → r90-15 detects bytes
  moved before a stale part.
- Mutation I: let Backlog use the normal section cap → r90-16 RED (titles leak
  on cold open).
- Mutation J: reset expansion in any refresh tail → r90-17 RED.
- Mutation K: omit explicit-lens force/auto-reveal → r90-18 RED.
- Mutation L: scan or timer from mark/bulk/collapse → r90-10 RED.

GUI confirmation remains useful for the perceived rapid =arrow, b, arrow, b=
loop, the =•= gutter face, side-rail count reflow, and pane repaint timing,
but all identity/call-order contracts are batch-pinned.

* Text fixtures / goldens

No existing text fixture should move:

- no committed fixture heading carries the actual =:backlog:= tag, so the new
  collapsed-body rule is dormant in every current dashboard golden;
- an empty mark set emits the exact old row indentation/banner/rail bytes;
- mark glyph/count/help additions are conditional or outside existing goldens;
- source/cache schemas do not change.

Do not add or re-bless a broad viewport golden merely to freeze ephemeral
selection state.  The focused R90 ERTs assert exact header/body/glyph/status
bytes for non-empty Backlog and marks.  If =make regen-mockups= shows any
existing diff, hold it as a bug: the expected fixture delta set is empty.

* Implementation surface and order

1. *Selection foundation* (=org-air-view.el=): source-key helper, ordered mark
   set/index/reconcile, render-derived mark prefix, =m=/=M=, conditional
   status/rail/help, installer registry entries.
2. *Local landing* (=org-air-view.el=): mutation landing-plan capture/consume,
   render-tail ordering before inspector setup, direct pane resync.  Route
   unmarked R83 =b= through it without changing its source/ring behavior.
3. *Bulk tag coordinator* (=org-air-view.el=, with the existing Org write
   helpers from =org-air-inbox.el=): marked dispatch, set-all =b=, shared-tag
   =t=, preflight, per-file groups/saves, cached tag/memo/source-position
   updates, one repaint and exact reports.
4. *Compound history* (=org-air-view.el=): =bulk= records/parts and R73/R75
   dispatch; normal record paths byte-untouched.
5. *Backlog collapse/lens* (=org-air-view.el=): central expanded predicate,
   header-only special case, filter/bookmark auto-reveal, show-knob/summary
   agreement.
6. *Docs/tests*: README updates, R83 flips, new R90 suite, clean regen proof,
   full =make clean && make check=.

No change is expected in =org-air-query.el=, =org-air-classify.el=,
=org-air-calendar.el=, =org-air-review.el=, item struct/cache version, faces,
or golden fixtures.  A tiny shared source-write helper may live in
=org-air-inbox.el= only if needed to avoid copying the R68 logging bindings;
no query or refile-engine behavior changes.

* Attractive deferrals

- Metadata-only marked =e=: shared TODO/priority/category/schedule/deadline
  values, tag add/remove deltas, no destination or note.
- Direct marked =T=/=d=/schedule and confirmed marked =D= after exact
  derived-slot synchronization is designed and tested.
- Bulk refile with a review screen, target preflight, and a true inverse move;
  bulk archive/kill with aggregate confirmation.
- Persistable named selections (not implicit session marks).
- Bounded/paged expanded Backlog for very large deferred sets.
- A reusable multi-file transactional save journal if org-air later needs
  stronger-than-per-file disk guarantees.

* Implementation History

- 2026-07-25: Round 90 design created.  Audited the shipped R53/R68/R73/R75/
  R83 pipelines, R18/R51 collapse machinery, R67/R71 item editor, README,
  current board/source-write code, and tests.  Decisions: mutation-specific
  local focus excludes moved source keys; marks become an exact source-key
  ordered set with =M= clear and conditional status/rail/help; marked =b=
  uses set-all semantics and marked =t= one shared add value; unsupported
  mutations block rather than act only on point; per-file atomic commits +
  one cached repaint/no scan; one compound undo record with an explicit
  cross-file non-atomic failure law; choose compact Backlog header/count,
  collapsed on cold open and auto-revealed by =is:backlog=.  Defined the
  acceptance matrix, 20 test seams, mutation/revert-RED plan, zero-golden
  expectation, implementation order, and scoped deferrals.  State: ready.

- 2026-07-25: Round 90 IMPLEMENTED.  Added the one-shot section-local
  mutation landing plan for single and marked =b=; exact buffer-local
  source-key marks with generation reconciliation, mirror rendering,
  =m=/=M=, total/shown banner and rail counts, conditional bulk Actions and
  help; Backlog header-only cold rendering with persistent =TAB= expansion
  and exact =is:backlog= reveal (including the knob-nil override); marked
  =b= set-all and marked =t= shared-prompt coordinators with complete
  preflight, deterministic descending heading writes and sorted file commits,
  stale/ineligible/failure accounting, cached source-position relocation,
  truthful mark clearing and one cached repaint; and compound multi-file
  =u=/=U= history with all-file preflight, per-file atomic steps and the
  specified partial-failure residual law.  Unsupported item mutations now
  guard before prompts/writes while marks are active.  Updated board key
  registration (review's lowercase =m= remains its span control),
  help/rail/banner surfaces, README, defcustom/docstrings,
  and the R83 known-failure manifest for the three design-obsolete cold-row
  assumptions.  Per seat ownership, no ERT or golden payload was added; the
  independent test seat owns the Round-90 acceptance tests and re-blessing.
  =make clean && make check=: 1102 tests, 1094 expected results, 3
  design-expected R83 failures, 8 GUI-only skips, 0 unexpected; lint clean;
  compile has only the pre-existing =org-air-project.el= wide-docstring
  warning.  =make regen-mockups= produced zero fixture churn.  Implementation
  complete; state remains work-in-progress for independent test/review
  closeout.

- 2026-07-26: Independent TEST/AUDIT completed RED/BLOCK.  Re-blessed the
  three design-obsolete R83 tests without weakening their source-byte,
  no-scan, co-tag, or round-trip assertions; all three now pass and the
  known-failures manifest is empty.  Added
  =tests/org-air-round90-test.el= with 25 ERTs covering r90-1..20 plus the
  supervisor's hostile A/B/C paths.  Focused current-implementation run:
  25 tests, 18 pass / 7 RED.  Full =make clean && make check=: 1127 tests,
  1112 expected, 7 unexpected, 8 skipped; compile succeeded and lint was
  clean.  Exact source blockers (left RED, never manifested):
  1. =org-air-r90-1-local-focus-middle-successor-no-scan= expected zero
     timer constructors but observed 3; r90-10 observed 6 and r90-19
     observed 2.  They are =org-element--cache-sync= idle timers armed by
     =org-toggle-tag= during the command, violating the acceptance plan's
     0-timer seam even though landing/final identities otherwise converge.
  2. =org-air-r90-12-inherited-effective-tags-do-not-reject-valid-heading=
     proves exact preflight rejects a valid inherited-tag child: cached
     effective tags are =inherited,local= while source-local tags are
     =local=, so the shared tag is not written, the mark remains, and no
     bulk history is created.
  3. Hostile A, =org-air-r90-15a-runtime-undo-retains-failed-current-part=:
     after c.org undo succeeds and b.org save fails, committed a.org and
     failed-current b.org must both remain in residual undo; actual residual
     contains only a.org because b.org was popped from =remaining=.
  4. =org-air-r90-15b-runtime-redo-has-truthful-residual=: after a.org redo
     succeeds and b.org save fails, b.org disk remains undone but its live
     buffer remains redone (=backlog=), violating same-file rollback and
     buffer/disk truth; no false redo branch is created and the a.org
     residual itself is otherwise correct.
  5. Hostile B, =org-air-r90-hostile-b-post-save-sync-cannot-diverge=:
     injected relocation failure after successful save yields the exact
     split =disk=t, live=nil, cached-tag=nil, later-position stale, mark=t,
     history=nil; expected a fully synchronized committed part.  Disk bytes
     therefore diverge from every live/cache/selection/history surface.
  Hostile C passes: no-op-only and stale-prune-only actions perform no source
  save and create no history.  Healthy same-file rollback, cross-file stop,
  relocation, compound u/U shapes, collapse/lens, marks, and discoverability
  seams pass.  Revert/mutation audit: the suite against pre-R90 was RED
  24/25 (only the pre-existing expansion-persistence r90-17 seam survived);
  the three re-blessed R83 tests were 2 pass / r83-4 RED on cold-row leakage;
  targeted mutation J (clear expansion at refresh-repaint) made r90-17 RED.
  =make regen-mockups= was zero churn.  Result remains red/block; source was
  not repaired by the test seat.

- 2026-07-26: Round 90 SOURCE CORRECTION.  Exact heading preflight and all
  write/undo/redo mirrors now compare and retain effective source tags while
  keeping =org-toggle-tag= local-only, so inherited tags are neither rejected
  nor duplicated onto children.  Compound history now snapshots only
  ephemeral per-attempt buffer/undo state, restores live bytes and undo
  machinery after an undo/redo or save signal, includes the restored failed
  current undo part plus every untouched part in canonical commit order, and
  never advertises a speculative redo.  Bulk writes and history operations
  precompute relocation/effective-tag state before save; after the irreversible
  save boundary, cache/Org-element/mark/history finalization catches the
  injectable relocation helper and applies that captured state directly, so
  disk, live buffer, cache positions/tags and history cannot split.
  The four data-integrity blockers passed twice consecutively in focused ERT.
  Timer investigation found no R90 application timer: a stock temporary
  =org-mode= buffer with a live element cache followed by bare
  =org-toggle-tag= constructed four idle timers, all with callback
  =org-element--cache-sync=.  The independent seams observe the same callback
  3/6/2 times in r90-1/r90-10/r90-19.  The pre-R90 implementation diff shows
  the R68 single-item =b= path already invoked the same =org-toggle-tag= through
  =org-air-view--at-item-source=; R90 landing/coordinator code contains no
  timer constructor.  No Org timer was canceled, rebound, suppressed or
  disabled.  The remaining timer assertions therefore need to narrow their
  spy to org-air-owned deferred repaint/pane/refresh constructors.  Final
  =make clean && make check=: compile succeeded (only the pre-existing
  project wide-docstring warning), lint 0, 1127 tests with 1116 expected,
  exactly those 3 timer-spy assertions unexpected, and 8 GUI skips.  The
  known-failures manifest is empty; =make regen-mockups= was a no-op; all test
  files are byte-pristine against the audit parent and fixture/golden movement
  is zero.

- 2026-07-26: Fresh GPT-5.6 Sol RETEST/AUDIT completed GREEN.  Narrowed the
  three timer seams without dropping timer coverage: both timer constructors
  now record constructor kind, delay, repeat, callback identity and callback
  arguments.  Exact callback =org-element--cache-sync= is classified as
  intrinsic Org element-cache maintenance; every callback rooted in org-air
  is independently rejected, as is every other non-intrinsic callback, with
  the full observation printed on failure.  There is no hard-coded total in
  which an application timer could hide.  The live observations remain 3/6/2
  idle constructors in r90-1/r90-10/r90-19, all
  =org-element--cache-sync=; zero-query/refresh, one-render and one direct-pane
  resync assertions remain pinned.  An injected =org-air-refresh= timer made
  r90-1 RED and printed its callback and =(:injected)= argument.
  Re-audited the four source corrections in two consecutive focused GREEN
  passes.  Strengthened inherited-tag coverage to assert exact local source
  bytes and exact effective cached tags after write/=u=/=U=; residual coverage
  now pins canonical part order and no speculative redo; undo/redo save
  failures pin disk/live bytes, modified state and undo-list identity; new
  r90-15c injects an undo-operation failure and proves an older unrelated undo
  remains usable; and hostile post-save relocation coverage now pins exact
  cached tags/positions, mark clearing, one truthful file part and successful
  =u=/=U=.  In an isolated workspace with the source correction reverted to
  =uqqpllvwnkon=, the inherited/residual/redo-save/post-save set was RED 4/4
  with the expected distinct blockers.  The original pre-R90 24/25 RED audit,
  re-blessed R83 cold-row RED, and collapse-reset mutation-J RED evidence above
  remain intact.  The strengthened R90 file has 26 ERTs (one new named case).
  Final =make clean && make check=: compile succeeded with only the pre-existing
  project wide-docstring warning, lint 0, 1128 tests / 1120 expected / 0
  unexpected / 8 GUI skips.  Manifest active=0; =make regen-mockups= produced
  byte-identical goldens; implementation =.el= files are pristine against
  =rnpvtwtmrmpq=; no probe file remains.

- 2026-07-26: REVIEW FIX 2 completed from the six distinguished-review RED
  probes (baseline 0/6).  Established one shared save-attempt boundary for
  single source writes, marked =b=/=t=, and compound =u=/=U=: temporary
  buffer-local hook sentinels run at Emacs-29 hook depths +100 before write
  preparation and -100 at after-save entry, are token-scoped for nested
  org-air attempts, and are removed unconditionally.  The boundary also has
  a conservative unmodified/visited/file-equality fallback for unusual file
  handlers.  Source edit groups now finish before =save-buffer=; only a true
  pre-write failure restores the ephemeral text, modified/visited state,
  undo list and undo walker, stops later files, and records no success.  Once
  after-save lifecycle begins, a later hook signal is committed success:
  live committed bytes remain, total cache/mark/history finalization runs
  exactly once, and a bounded warning is surfaced.  No source snapshot is
  retained in either history ring.

  Touched-file preflight now builds a total exact relocation set before the
  first mutation.  User-visited source buffers keep separate ephemeral live
  locators (durable item/row identities remain numeric conses), allowing
  unsaved insertion drift to map exactly without title search; otherwise an
  incomplete locator rejects the whole file with =run g r=.  Relocation
  markers stay before Org's own heading rewrite, then switch insertion type
  immediately before save hooks.  A latest pre-write capture therefore reads
  positions plus local/effective tags after all ordinary =before-save-hook=
  edits.  Finalization iterates every relocation directly, retains inherited
  versus local tag truth, and remains total even when the normal relocation
  helper signals.  Day/permissive landing now first restores the exact key in
  the old rendered section; exclusion applies only after it actually leaves,
  preserving ordinary board successor/previous behavior.

  Proof: the supplied real-hook review probes are GREEN 6/6 (single =b=,
  marked =b=, before-save relocation, pre-existing unsaved drift, day focus,
  compound =u=); supplementary read-only probes are GREEN 3/3 for marked
  =t=, compound =U=, and marked day focus.  Focused R90 is 26/26.  Final
  =make clean && make check= is GREEN: compile succeeds with only the
  pre-existing project wide-docstring warning, lint 0, 1128 tests / 1120
  expected / 0 unexpected / 8 GUI skips; manifest active=0.
  =make regen-mockups= is a no-op, tests/README are byte-pristine against
  =lsszzqumkswv=, golden movement is zero, and no repository =probe-*.el=
  exists.

- 2026-07-26: REVIEW FIX 2 RETEST/AUDIT 2 completed GREEN.  Landed the six
  distinguished-review probe contracts as permanent, stronger normal R90
  coverage in =tests/org-air-round90-test.el=, plus the three supplementary
  =t=/=U=/marked-day and save-boundary hygiene shapes: nine new ERTs
  =r90-21..29=.  The tests now pin a real post-write =after-save-hook= signal
  for single =b=, marked =b=/=t=, and both compound =u=/=U= directions;
  exact disk/live/effective-cache/locator/mark/history/ring truth; one prompt,
  one file part and no duplicate/retry; bounded warnings; classify
  invalidation; final point/pane/inline-rail inspector identity; real
  =before-save-hook= relocation followed by a second no-scan action; exact
  pre-existing unsaved-drift tracking without title fallback; whole-file
  pre-write rejection of an untracked incomplete locator with =run g r=;
  unmarked and marked Scheduled/Deadline day focus; exact pre-write recovery
  of bytes, modified/visited/undo state; temporary hook cleanup after normal,
  pre-write-failed, post-write-signalled and nested/reentrant attempts; and
  exact history metadata shapes with no retained source snapshot.  The
  identity-based timer seam is unchanged: only exact
  =org-element--cache-sync= is intrinsic, and every org-air or other callback
  remains RED.

  Current-source proof: new seams 9/9 GREEN; complete R90 35/35 GREEN; the
  supplied external review probe remains 6/6 GREEN.  Reverting only
  =qvwkmsunuoup='s =org-air-view.el= source changes to =lsszzqumkswv= while
  retaining the tests made the new set 0/9 GREEN, 9/9 RED for the intended
  post-write warning/commit, live rollback split, relocation/tracking,
  incomplete-locator, day-focus and absent save-attempt-boundary reasons.
  Prior evidence remains preserved: pre-R90 24/25 RED, the re-blessed R83
  cold-row RED, mutation-J expansion-reset RED, and the fix-1 correction
  revert set 4/4 RED.

  Final gate after a clean rebuild: 1137 tests / 1129 expected / 0
  unexpected / 8 GUI skips; lint 0; manifest active=0.  Focused new tests
  are 9/9 and all R90 tests are 35/35.  =make regen-mockups= produced zero
  churn.  Fixtures/goldens and README are byte-identical, every implementation
  =.el= is pristine against =qvwkmsunuoup=, and no repository =probe-*.el=
  exists.

- 2026-07-26: REVIEW FIX 3 completed from the five pass-2 blocker contracts.
  The earliest after-save sentinel now closes the org-air undo group and
  captures its exact undo/redo list-tail identity and chars tick before any
  ordinary hook.  A later mutating hook is represented by an explicit small
  =:expected-undo= field only while its user step is ahead: =u=/=U= and every
  compound preflight then move zero bytes, save nothing, and retain the record
  on the same ring until Emacs' exact undo-equivalence chain reaches that
  identity.  Independently undoing the hook edit makes the org-air step safe;
  no title/text guess or source snapshot is retained.  Normal R73/R75 records
  preserve their established shape, stale-edit consumption, deep-walk
  restamping, messages and redo backstop.  Single metadata cache-sync intent
  is held in a weak record-identity table containing no source data.  Hook
  failures also reach =display-warning= before the concise final success echo,
  so the diagnostic remains discoverable.

  Query generation and source validation/writing now share one broad heading
  projection in =org-air-query.el=: custom TODO/priority/COMMENT prefixes,
  plain title, writer-accepted hyphenated local tags, inherited parent and
  =FILETAGS= values, inheritance exclusions, and local-wins deduplication all
  agree.  The local writer never copies inherited values onto a child.  Since
  old persisted entries can contain a tag suffix in title with a nil tag slot,
  the disk cache schema is intentionally bumped from v6 to v7.  Post-commit
  cache finalization now isolates mandatory numeric marker/effective-tag slot
  writes, per-item classify invalidation, and cosmetic Org-element mirroring;
  parser failure cannot skip =remhash=, hostile =remhash= poisons the whole
  classify memo, and a mandatory-slot failure invalidates the complete item
  generation with a persistent warning instead of retaining wrong targets.

  Global source tracking now has one explicit canonical board owner.  Only
  that owner installs/removes =find-file-hook=; incidental mode buffers cannot
  steal or tear down tracking, source kill/owner reopen are symmetric, and
  owner-scoped teardown touches no unrelated buffer.  Every generation swap
  first releases stale item markers and duplicate current entries, then
  hydrates only already-live source buffers, preserving the R53 no-open/no-scan
  repaint law.

  Proof: the three internally consistent supplied probes (finalization,
  hook-owner, generation release) are GREEN 3/3.  Contract-correct temporary
  probes are GREEN for the other two blockers: a mutating/signalling hook
  blocks =u= with exact zero-byte retry and succeeds after independent hook
  undo, while a real hyphen-tag refresh keeps title =Alpha task= (not the tag
  suffix) plus tag =shared-tag= and permits a second exact write.  Supplementary
  temporary probes are GREEN 4/4 for compound mutating hooks in both
  directions, hyphenated local/parent/FILETAGS inheritance and second mutation,
  hostile parser/=remhash=/mandatory-slot fallback, owner kill/reopen and
  unrelated source isolation, and twelve repeated generation replacements.
  Two additional compound resolve probes are GREEN 2/2 after independently
  undoing the ahead hook step, and deep same-buffer single/compound histories
  are GREEN 2/2: an older record also blocks rather than consuming the user's
  edit, then resumes at its exact identity after resolution.  The two original
  supplied files remain assertion-RED by construction: one
  demands immediate org-air undo while the user hook edit is still ahead,
  contrary to the zero-byte blocking requirement; the other demands the known
  malformed title =Alpha task :shared-tag:=, contrary to the plain-title
  requirement.  Their diagnostic state confirms the required behavior.

  Focused R90 is 35/35 GREEN; the seven historical R73/R75 compatibility
  regressions are restored GREEN 7/7.  Clean =make check= compiles (only the
  pre-existing project wide-docstring warning), lints with 0 findings, and
  runs 1137 tests with 1127 expected, exactly 2 schema-expectation failures and
  8 GUI skips.  The only failures are
  =org-air-r59-13-cache-v5-and-key= and
  =org-air-r86-18-no-cache-version-bump=, both pinning cache version 6; every
  behavioral test is green.  Per seat ownership those stale schema assertions
  were not edited.  =make regen-mockups= produces no fixture diff; tests,
  fixtures, known-failures and README remain byte-pristine, golden movement is
  zero, and no repository probe file exists.

- 2026-07-26: Independent TEST/AUDIT FIX-3 stopped BLOCKED at the mandatory
  bounded-history audit.  A real mutating/signalling buffer-local
  =after-save-hook= correctly leaves its user edit ahead and makes immediate
  org-air =u= a zero-byte retry, but the exceptional ring record's
  =:expected-undo= is the exact cons tail of =buffer-undo-list=, not small
  identity metadata.  Before that org-air write, the audit deleted and saved a
  distinctive 128 KiB source line in an unrelated older undo group.  Traversing
  the ring record after removing its one accepted =:buffer= reference, killing
  the source buffer and forcing GC still reached that raw undo-list tail and the
  deleted source string: 36 conses, 4 strings,
  131188 total string bytes, largest string 131120 bytes, both
  =:undo-tail-reachable= and =:distinctive-source-reachable= true.  Thus the
  global ring can retain an unbounded undo chain and deleted source contents
  after source-buffer death, contrary to the no-snapshot/bounded-metadata
  requirement.

  Permanently added one RED ERT,
  =org-air-r90-30-history-identity-is-bounded-and-source-free=, which constructs
  that real history and audits the complete transitive record metadata after
  removing only the accepted buffer field.  It rejects any further buffer or
  marker, any raw undo-list-tail alias, and retained distinctive source bytes,
  with loose bounds of 256 conses / 8192 string bytes to avoid coupling to a
  future bounded identity representation.
  Focused R90 is 35/36 expected with only r90-30 RED and prints the exact graph
  totals above.  Full clean gate compiles with only the pre-existing project
  wide-docstring warning, lints 0, and runs 1138 tests / 1127 expected / 3
  unexpected / 8 GUI skips: r90-30 plus the two intentionally unedited stale
  v6 assertions =org-air-r59-13-cache-v5-and-key= and
  =org-air-r86-18-no-cache-version-bump=.  Manifest active=0.

  Pre-edit reproduction otherwise matched supervisor evidence: original review
  probes 6/6, finalization/owner/generation 3/3, corrected critical 1/1,
  supplementary 4/4, compound resolution 2/2 and deep history 2/2.  The two
  externally supplied contrary assertions remained intentionally RED: immediate
  org-air undo while a hook edit is ahead, and malformed refreshed title
  =Alpha task :shared-tag:=.  Per the explicit STOP instruction, no cache-v7
  re-bless, broader FIX-3 permanent coverage or source-revert audit was
  attempted after proving this blocker.  Only this RED test evidence and R90
  history changed; implementation =.el=, README, fixtures and goldens remain
  byte-pristine against =vptuoslynoqo=, and no repository probe file exists.

- 2026-07-26: REVIEW FIX 4 replaced every newly persisted undo identity with a
  bounded opaque =org-air-view--history-token=.  Exceptional
  =:expected-undo= fields now tokenise the exact undo-list tail; every new R90
  compound =:undo-head= field tokenises the same Emacs-owned tail with a
  head-projection, so neither path stores the raw tail/head object.  Active
  =save-result= plists may still carry raw tails only inside the dynamic command
  scope.  Legacy/synthetic raw =:expected-undo= and =:undo-head= values remain
  distinguishable from the struct token and retain the R73/R75 comparison law.

  Exact resolution uses one side registry with Emacs weakness
  =key-and-value=.  This is the required both-independently-live law: a mapping
  remains only while the history ring strongly owns its token /and/ Emacs' undo
  machinery strongly owns the raw tail; loss of either side removes the mapping
  on GC.  Missing token resolution never guesses from title, text, tick, or raw
  head equality: exceptional single and compound operations stay blocked with
  zero bytes, and normal compound preflight reports the missing history step.
  Source-buffer kill hooks eagerly forget that buffer's mappings.  Ring clear,
  truncation, stale/dead/structural consumption, failed residual splitting, and
  explicit record discard eagerly release token mappings and weak cache-sync
  keys; the weak semantics backstop every lifecycle path.

  Evidence: unchanged permanent r90-30 is GREEN.  A compound 128-KiB deletion +
  mutating/signalling-hook probe traversed the complete record/part graph with
  only documented =:buffer= references removed: 21 conses, 2 strings / 55
  string bytes, zero buffers/markers, and both raw-tail and distinctive-source
  reachability nil; both =:expected-undo= and =:undo-head= were bounded tokens.
  Killing the source, dropping probe-held undo references and forcing GC reduced
  the identity registry from 2 to baseline 0; 80 repeated create/discard cycles
  stayed at 0.  Forced GC at blocked state, after independent hook undo, and
  immediately before retry preserved exact success at the true identity (with
  safe zero-byte blocking permitted if Emacs drops it).  The unchanged critical,
  supplementary, compound-resolution, deep-history and review probes are GREEN
  1/1 + 4/4 + 2/2 + 2/2 + 6/6; the three consistent pass-2 probes are GREEN
  3/3.  Replacing token registration with FIX-3 raw tail/head persistence makes
  unchanged r90-30 RED with =:undo-tail-reachable t=,
  =:distinctive-source-reachable t=, 131188 string bytes and a 131120-byte
  largest source string.  Clean compile has only the pre-existing project
  docstring warning; lint is 0.  Full gate: 1138 tests / 1128 expected / exactly
  2 unexpected / 8 GUI skips; r90-30 and every behavioral test pass, with only
  the intentionally untouched stale-v6 assertions =org-air-r59-13-cache-v5-and-key=
  and =org-air-r86-18-no-cache-version-bump= failing.  Manifest remains empty.
  README/tests/fixtures/goldens remain test-seat owned and untouched.

- 2026-07-26: REVIEW FIX 5 closed the shared-tag input-safety blocker with
  one projection-adjacent single-value grammar: a tag is nonempty and contains
  no colon or whitespace (including space, tab, newline, carriage return and
  vertical whitespace); hyphens and every other non-colon/non-whitespace
  spelling remain accepted.  Marked and unmarked =t= validate immediately
  after their one prompt.  The marked coordinator repeats the check before
  landing capture/source preflight for direct callers, the low-level local-tag
  toggle refuses before moving point or changing bytes, and the pure heading
  line setter validates every local value before concatenation.  Backlog and
  inherited/local semantics are unchanged; inherited values are still never
  copied local.  This validates future writes only and leaves cache v7 intact.

  A temporary assertion probe is GREEN 4/4.  Thirteen invalid shapes (empty,
  space, embedded/leading/trailing space or tab/newline, and colon) each raise
  the concise =user-error= “Tag must be nonempty and contain no colon or
  whitespace” before preflight/open/save/render/pane/timer activity, while
  preserving exact disk/live/cache item vectors and identities, every mark,
  both nonempty history-ring sentinels, board point, selected window, window
  layout, pane context, inspector and pending landing.  Direct low-level
  toggle and line-set calls likewise refuse with zero line/point change.
  Valid =shared-tag=, =@context=, =hash#tag=, =under_score= and Unicode
  =日本語= each write, survive a real refresh as plain title plus exact tag,
  and permit a second exact backlog mutation and refresh.  With the shared
  validator deliberately bypassed, the same probe recreates and detects the
  historical =:one::= and =:one:bad tag:= disk corruption for empty/space
  input, proving the assertions are mutation-sensitive.

  Permanent R90 is GREEN 36/36 including r90-30; original review is 6/6;
  consistent pass-2 finalization/owner/generation is 3/3; FIX-3 critical,
  supplementary, compound-resolution and deep-history probes are respectively
  1/1, 4/4, 2/2 and 2/2.  Clean compile retains only the pre-existing project
  wide-docstring warning and lint is 0.  Full gate runs 1138 tests with 1128
  expected, exactly the two intentionally untouched stale-v6 assertions
  =org-air-r59-13-cache-v5-and-key= and
  =org-air-r86-18-no-cache-version-bump= unexpected, and 8 GUI skips; every
  behavioral test is green.  =make regen-mockups= is a byte no-op.  Tests,
  README, known-failures, fixtures and goldens remain byte-pristine against
  =oqnuxmmkopwl=; golden movement is zero and no repository =probe-*.el=
  exists.

- 2026-07-26: FINAL INDEPENDENT TEST/AUDIT RETEST 4 completed GREEN.
  Re-blessed the two semantically stale cache assertions as current v7
  contracts: =org-air-r59-13-cache-v7-and-key= retains every historical
  struct/key/coherence fence, and
  =org-air-r86-18-path-adds-no-cache-coherence-input= truthfully says R86
  adds no path-specific coherence input while acknowledging R90's independent
  v7 transition.  New =r90-31= serialises the former malformed v6
  =title "Alpha task :shared-tag:" / tags nil= shape under the current key and
  proves both cache APIs return a clean miss; a real v7 write/read preserves
  plain title plus =shared-tag=.  Changing only the source constant back to 6
  made the selected cache set RED 3/3; r90-31 failed specifically because the
  malformed v6 object hydrated.

  Added 15 permanent ERTs =r90-31..45= (R90 now 51/51) using shared helpers.
  The save/history set pins real mutating+signalling after-save hooks for
  single =b=, initial marked =b=/=t=, compound =u=/=U= directions and deep
  older single/compound records: committed disk truth plus legitimate unsaved
  hook text, modified live state, persistent warning, exact cache/ring facts,
  zero-save/zero-byte same-ring blocking, user-only hook resolution, honest
  retry/redo, no speculative branch/reorder, and sentinel cleanup.  The
  compound retention audit requires both =:expected-undo= and =:undo-head= to
  be bounded =org-air-view--history-token=s; transitive metadata with only
  documented buffer references removed reaches no raw tail/head, marker or
  distinctive 128-KiB source text.  Empirical =key-and-value= checks drop a
  mapping when either independently held key or value disappears; source kill,
  80 truncate/clear cycles and GC return the registry to zero.  Forced GC on a
  blocked→resolved compound retry either follows the exact identity or stays
  zero-byte blocked; current source follows the exact step.

  The projection/input set permanently covers hyphenated local, parent and
  FILETAGS values; local-wins deduplication; exclusions plus nil/list/regexp
  inheritance; no inherited-to-local copying; custom TODO/priority/COMMENT
  prefix preservation; marked =shared-tag= and =@context=/underscore/Unicode
  through real generation swaps and a second no-scan write.  Thirteen empty,
  colon and embedded/leading/trailing space/tab/newline values are tested in
  both marked and unmarked dispatch: exactly one prompt, immediate =user-error=
  and byte/object-identical disk, live cache, marks, rings, focus, inspector,
  panes, windows and timers.  Low-level toggle/line setters are zero-change too.
  Bypassing the validator made both permanent invalid-input ERTs RED 2/2.

  Total-finalization tests isolate combined relocation-helper/cosmetic-parser
  failure, hostile =remhash= and one mandatory slot failure among several
  items.  Mandatory slots and every warmed classify entry still invalidate;
  =remhash= failure poisons the whole memo; mandatory failure drops generation,
  marks and old locators, persists a warning, performs a truthful safe rebuild,
  and the next cached command reaches only the exact target.  Owner lifecycle
  tests cover source open before/after board, incidental mode open/kill,
  source kill/reopen, owner kill/reopen over already-live sources,
  cross-project isolation, exactly one global hook, and twelve no-open/no-query/
  no-refresh/no-org-air-timer generation swaps with dead old markers, one
  current locator per item and numeric durable keys.  Reverting view/query to
  pass-2 =nmtxuvkxmkny= made the selected FIX-3 set RED 10/10; within it the
  old broad finalizer was RED 3/3 and old owner/prune lifecycle RED 2/2.
  Reverting bounded storage to raw-tail =oxyrryyqprum= made r90-30 plus the
  compound/registry audits RED 3/3: raw tail/head and 131188 source-string
  bytes were reachable, and token/registry APIs were absent.

  Current-source evidence: all distinguished probes are GREEN (original review
  6/6; consistent finalizer/owner/generation 3/3; FIX-3 critical,
  supplementary, resolve and deep =1+4+2+2=; FIX-5 malformed/valid 4/4).
  Relevant R53/R59/R68/R73/R75/R83/R86/R90 is 153/153.  Final clean gate:
  1153 tests / 1145 expected / 0 unexpected / 8 GUI skips; lint 0; manifest
  active=0.  =make regen-mockups= is a no-op.  Implementation =.el= is
  byte-pristine against =krpmszyxpmvo=; README, fixtures and goldens are
  byte-identical; no repository probe exists and =.deps= remains present.

* History

- 2026-07-25: Design completed and set ready for implementation.
- 2026-07-25: State: draft → ready.
- 2026-07-25: State: ready → work-in-progress.
- 2026-07-25: Implementation completed and gated; state remains
  work-in-progress pending independent test/review closeout.