#+title: org-air v0.2.x: interactive UX hardening
#+state: complete
#+FILETAGS: :ui:ux:bugs:

* Summary
Punch list from the user's interactive GUI test of the v0.2 main line.
Verbatim feedback: "It's using only vertical space, and not horizontal
space at all. Also, when Evil binding is set, nothing actually gets
triggered unless I prefix with \ or turn off evil. When a new item is
added, it should automatically refresh the view. The calendar view
actually looks nice, but it's too far to the right and it's off the
visible area. It's all so rough, UX is horrible."

* Findings / required fixes
** U1 — Live-window width derivation is wrong [CRITICAL]
Symptoms combine: items render vertically-stacked while the calendar
rail sits beyond the right edge of the visible window. Hypothesis: the
renderer composes against the wrong width in real GUI use (likely the
selected window at render time vs the window the buffer is displayed
in, or stale width on re-render). Required: compose against the width
of the window actually displaying *org-air* at display time; re-render
on window-size-change/window-configuration-change (debounced); never
emit lines wider than the displaying window.
** U2 — Evil compatibility [CRITICAL]
With evil-mode active, dashboard keys do nothing without a "\" prefix.
Required: bindings must work out of the box under evil — overriding
keymap for org-air-view-mode (evil-make-overriding-map + motion/normal
state handling, or documented evil-set-initial-state 'emacs) WITHOUT
breaking non-evil users; soft dependency only (no hard require of evil).
** U3 — Auto-refresh on data change [HIGH]
New items (org-air-capture, refile, or edits to org-air-files saved
from org buffers) must refresh an open dashboard automatically
(after-save hook scoped to configured files + post-capture/refile
refresh). Preserve point/filters across refresh.
** U4 — General UX roughness [HIGH]
Design track: an interactive-feel pass — real window sizes (not just
fixture widths), spacing, first-open experience, point placement,
visual hierarchy at common sizes (e.g. 90-110 col windows where the
two-pane threshold sits). Concrete spec deltas, then impl.

* History
- 2026-06-12: Seeded verbatim from user interactive test of v0.2.
- 2026-06-13: U1 root cause: width derived in PIXELS of the selected
  window; now columns of the displaying window, render-after-display,
  debounced re-render. U2 evil overriding-map (soft dep). U3 scoped
  after-save auto-refresh with point/filter preservation. Design D1-D7
  real-window deltas implemented (impl2). Fixtures regenerated honestly
  at 8 widths, design-blessed byte-for-byte. Integrated; gate 103/103.
- Residual backlog: D2 origin clip on longest [#A] rows at 100 cols
  (non-blocking polish).

* Round 2 findings — user screenshot (2026-06-13, /tmp/org-air-ace/user-screenshot-1.png)
User verdict: "It looks very broken, not sophisticated and modern at all."
** S1 [CRIT/bug] Header rendered TWICE (header-line + in-buffer band); second
   right-aligned status clipped off-screen (live D7 violation). Old
   header-line path must be fully removed; right-status composed to the
   displaying window width.
** S2 [HIGH/visual] Horizontal rules render as solid filled bars (face
   background bleeding full-width), not thin faded rules.
** S3 [HIGH/visual] Count badges are illegible inverse-video chips; need the
   quiet nano-style treatment (design to respecify against GUI reality).
** S4 [CRIT/bug] Needs-attention count=3 (badge + summary) while the section
   body shows the empty state — classification vs section-render disagree.
** S5 [MED/visual] Tofu/missing glyph before first item (glyph fallback or
   cursor-on-tofu; verify GUI font coverage for chosen glyphs).
** S6 [HIGH/design] Vertical composition: content top 40%, floating footer,
   dead void below — design the intentional full-height behaviour.

* Round 3 closeout (2026-06-13)
- Demo dataset (examples/demo + /tmp/org-air). Triage: dual-membership
  classify (dated inbox items appear in Inbox AND their date bucket;
  summary total = unique), inline dispositions, guided process-inbox
  (I, count-down to inbox zero), s/S keymap remap, 'scheduled · file
  with r' nudge. S7 header W-1 margin + global usable-columns clip fix.
  S8 line-spacing 0. S9 AA-legible calendar legend (samples in real
  cell faces). Cursor-on-visible-char across all point-moving paths.
- Integrated rwoskzmn; gate 121/121. Test seat reassigned Fable→Opus.
- Backlog: D2 origin clip @100 (non-blocking).
