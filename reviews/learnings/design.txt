[org-air.work.design.learnings:36103] {"agent":"org-air-design","scope":"cross-round design role, org-air codebase","learnings":["VERIFY THE INTEGRATED UI, NOT YOUR ARTIFACT IN ISOLATION. The single biggest lesson: a faces library can byte-compile clean, pass facep, and have perfect light/dark specs while being 100% dead code because the renderer shadowed every face with its own local deffaces and never (require ´org-air-faces). R1 verified the library alone and missed this; R2 caught it only by rendering the real dashboard and dumping face text-properties. For a design/visual role here, your verification MUST be: render org-air-view--render against tests/fixtures and walk next-single-property-change to list the faces ACTUALLY applied. Anything else is theater.","FROZEN-CLOCK RENDER RECIPE (reuse verbatim each round). org-ql is not vendored: run make deps (network) -> installs to repo-local .deps/ (gitignored). Then: emacs -Q --batch -L tests -l tests/org-air-test-init.el -l render-dump.el, where render-dump cl-letf´s current-time to org-air-test-now (Mon 2026-06-15 10:00, from tests/org-air-test-helpers.el) and wraps everything in org-air-test-with-fixtures. Both classify and view read (current-time) directly, so freezing it makes overdue/upcoming/calendar deterministic. Dump (a) buffer-substring-no-properties, (b) face runs via next-single-property-change, (c) distinct faces set. This recipe found a one-operator sign-inversion bug (overdue<->future) that a screenshot or a passing unit test would not surface.","WATCH FOR SIGN/ARG-ORDER BUGS IN DATE MATH. org-air has two days-between helpers (classify + view) with the SAME name pattern but the convention is now-minus-then (positive=past). Callers that want future-positive must swap args (human-date does: (days-between now time)). The overdue branch got it wrong (used < where > is needed) so EVERY dated row was mislabeled while unit tests stayed green. When reviewing dates, build a fixture table of known past/future items and check the rendered label+face against it, don´t trust the code reading right.","JJ SANDBOX INCANTATION IS STABLE AND ESSENTIAL: prefix every jj with GIT_CONFIG_GLOBAL=/dev/null and --config signing.behavior=drop. jj auto-snapshots on EVERY command (even st), and without these it dies on a permission-denied yubikey git include + GPG signing. Scope commits with jj commit <paths> -m ´...´ -> commits only those filesets and parks the rest in a fresh empty @; no jj new needed. This worked first-try every round once known.","EVENT/COMMS CONVENTIONS: ever topics are NOT auto-created — always ever topic create <topic> before pub (idempotent, swallow stderr). The DONE event is the orchestrator´s wake-up signal — publishing only status leaves it blocked (I made this mistake once). Topic names use the instance segment without project prefix: org-air.work.design.status / .done / .learnings. Mid-project the human reassigned the org-air-orchestrator crosstalk handle, so reach the orchestrator via chronoa send org-air-main, NOT pi-crosstalk. Always send a chronoa summary alongside the .done event.","PROMPT IMPROVEMENTS FOR THE NEXT DESIGN AGENT: (1) state the trunk merge change id up front each round (orchestrator did this well); (2) name exact topic strings + ´create then pub´ + ´remember the .done wake-up´; (3) for re-review rounds, say ´confirm the prior fix landed via grep + re-render, focus on deltas´ rather than re-deriving the whole review — this kept R3/R4 tight; (4) carry the frozen-clock recipe forward so it isn´t rediscovered; (5) keep scope lines explicit (reviews/ + faces + design doc) — made scope-clean commits trivial. Air convention: design docs go ready->complete only after the UI is verified implemented; record each review round in the doc History and keep reviews/design-review-rN.org per round."]}
[org-air.work.org-air-design.learnings:35957] {"agent":"org-air-design","learnings":["jj auto-snapshots on EVERY command and signing was enabled globally, so even `jj st` failed with a GPG error. The recycle prompt must say: prefix all jj invocations with `GIT_CONFIG_GLOBAL=/dev/null` AND `--config signing.behavior=drop` — both are needed (one for the blocked yubikey-status include, one to skip signing).","The global git config pulled in `~/.config/git/yubikey-status` which is permission-denied in the sandbox, breaking jj backend init entirely. Future prompts should pre-warn that GIT_CONFIG_GLOBAL must be neutralized.","`ever` topics are not auto-created — `ever pub` returns `topic not found` until you run `ever topic create <topic>` first. The prompt should include the create step (or say topics may need creating) for each status/done/learnings topic.","Scope discipline worked cleanly: `jj commit <paths> -m ...` commits only the listed filesets and pushes the rest (SUMMARY.md scratch) into a fresh @, so no extra `jj new` was needed. Worth keeping this pattern in the next prompt.","Predecessor`s SUMMARY.md was accurate and made handoff trivial — the missing context at start was purely the exact sandbox-safe jj invocation; deliverables and verification were already done. Next round: lead with the working jj command so a recycled worker isn`t rediscovering it."]}
[org-air.work.org-air-design.learnings:36051] {"agent":"org-air-design","round":2,"learnings":["R1 learnings held up perfectly: the GIT_CONFIG_GLOBAL=/dev/null + --config signing.behavior=drop combo worked first try on every jj command, and the `jj commit <paths>` scoping pattern again parked nothing extra. The one R1 gap: topics. R2 used DIFFERENT topic names than R1 (org-air.work.design.status vs org-air.work.org-air-design.status) and they still needed creating — so the lesson is broader: ALWAYS `ever topic create` defensively before every pub, regardless of name.","Biggest substantive trap this round: a clean byte-compile and `facep`-passes-after-load tells you NOTHING about whether the UI actually uses your faces. R1 verified the faces library in isolation; the integrated renderer defined its own 6 fallback deffaces and never required org-air-faces.el, so the entire palette was dead code. Design verification MUST render the real dashboard and dump face text-properties, not just load the library.","New tooling note for headless review: org-ql is not vendored — `make deps` (network) installs it into repo-local .deps/ (gitignored), then render with the clock frozen via cl-letf on current-time to org-air-test-now (Mon 2026-06-15 10:00) for deterministic classification + calendar output. next-single-property-change over the buffer gives a clean face-run dump.","Round-3 prompt should: (1) state the trunk merge change id up front; (2) name the EXACT topic strings for status/done/learnings AND say to create-then-pub each; (3) if r3 is a re-review, tell the worker to first confirm the BLOCKER fix landed — grep that org-air-view.el/org-air-calendar.el now (require ´org-air-faces) and re-render to confirm tag accents/semantic dates/count badges actually appear, rather than re-deriving the whole review; (4) carry forward the frozen-clock render recipe so the worker doesn´t rediscover make deps + cl-letf."]}
