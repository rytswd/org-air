[org-air.work.impl.learnings:36099] {"agent":"org-air-impl","learnings":["Always start from the exact base/merge change in the prompt and confirm whether prior worker commits are superseded or should be merged; several rounds depended on combining implementation, review, and test-track changes in the right order.","Use the sandbox-safe jj pattern everywhere: GIT_CONFIG_GLOBAL=/dev/null jj --config signing.behavior=drop ..., and clean stale bytecode before verification (make clean or rm -f *.elc tests/*.elc) because .elc files can hide source fixes.","Org APIs have sharp edges: org-get-tags LOCAL hides #+filetags, org-get-heading can strip priority cookies, org-get-priority returns cooked numeric scores, org-done-keywords is buffer-local, and date math should use calendar-day semantics rather than raw seconds.","For this package, treat org-air.el as the package-lint main file for the multi-file package; subfiles need package-lint-main-file locals, no Package-Requires headers, checkdoc-clean docstrings, and lint baseline entries must be deleted as findings are fixed.","Performance convention that worked: keep refresh as one org-ql pass, cache dashboard items in the view buffer, and re-render from cache for filter/scope UI changes unless explicitly refreshing from disk.","Future prompts should name exact ever topics, base changes, allowed files, expected test count/gate (make check vs make test), known parallel artifacts, and whether design-spec placeholders such as calendar paging/date scoping/bulk refile are in or out of scope."]}
[org-air.work.org-air-impl.learnings:35955] {"agent":"org-air-impl","learnings":["Initial implementation lacked the sibling test contract context; next prompt should include/merge tests before first commit so public API exposure is verified via (require org-air).","Git/JJ commands needed GIT_CONFIG_GLOBAL=/dev/null plus --config signing.behavior=drop to avoid inaccessible signing/GPG config; keep this explicit for all jj status/commit/squash operations.","Org API details were easy to misread: org-get-tags LOCAL excludes filetags, org-get-heading can strip priority cookies, and org-get-priority returns cooked numeric scores rather than raw priority chars.","Date classification should use calendar-day semantics (time-to-days), not raw 86400-second floors, or same-day deadlines/schedules disappear depending on current time.","Next round prompt should state whether stale detection should consider inactive timestamps in subtree; tests expected bare timestamps like [2025-11-02 Sun] to count as last activity."]}
[org-air.work.org-air-impl.learnings:36050] {"agent":"org-air-impl","learnings":["Round-1 learnings were right about stale .elc cleanup, jj sandbox flags, calendar-day semantics, filetags, and priority cooked values; those remained recurring failure modes.","Round-1 understated branch/base coordination: I initially built r2 on trunk pywloool while review fixes expected wokptompkzyz, so prompts should say whether to incorporate review/faces changes before coding and how to supersede prior commits.","New trap: org-done-keywords is buffer-local and can be nil outside org buffers; classification must derive state semantics from the item marker/file buffer or fall back to default/DONE, matching how query captured :todo.","Verification merges can include known parallel test artifacts; round-3 prompts should identify expected failing artifact tests versus must-pass target tests, while still requiring compile clean and no stale bytecode.","Round-3 prompt should include exact UX contract deltas, required base change ids, topic names, scope boundaries, and whether partial interactive placeholders (calendar paging, scope/date filtering, bulk refile) must become fully functional."]}
