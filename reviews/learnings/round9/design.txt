[org-air3.work.design.learnings:36982] {"agent":"org-air-design","round":9,"scope":"cross-round design role, org-air codebase","learnings":["RENDER-POSITION A/B CHOICES MUST BE SETTLED IN ONE AUTHORITATIVE MESSAGE. Round-9 D5 had a flush-col-0 vs spine-aligned rail-rule question. My bless commits and the orchestrator’s A/B question crossed in flight: an early spine bless (qtskrnpm) was written before I could see the flush render, then I ruled+blessed flush (kosuvqvy/slylkryu) and said A twice — but the orchestrator had a GREEN-READY spine set and integrated that. Lesson: when a visual choice is live, do not let intermediate ratifications imply a tip; state the single final pick + exact change ids in ONE message, and call out explicitly that earlier blesses are VOID. Whichever variant is green-and-ready tends to win at integration time, so bless the one you actually want EARLY.","BOTH VARIANTS WERE SPEC-VALID, WHICH MADE THE DELTA LOW-STAKES — recognise that early and avoid re-litigating a closed/integrated/tested round. The flush vs spine difference was purely the rule’s left edge (col 0 full-width vs inset-with-content); both passed 177/177. When the gap is cosmetic and one side is already integrated, accept the close and DOCUMENT the preferred variant as a future-fix rather than forcing a re-integration.","VERIFY THE INTEGRATED RAIL, NOT THE FACE ARTIFACT (still the #1 lesson). D5 re-bless was done right by dumping the FROZEN-CLOCK integrated render face-runs (next-single-property-change) and confirming org-air-face-rail-key actually lands on every Actions key token (c / s g S ?) with faded descriptions — not just facep-after-load. Also confirmed compose-level padding changes (flush vs spine) do NOT touch faces because the wiring lives in insert-rail, so a face-run bless survives a layout-margin change.","RECONCILE THE SPEC DOC TO WHAT SHIPPED. The D5b body asserted ‘rules flush at col 0’ but main shipped spine-aligned; left unreconciled that would mislead the next design agent. After integration, edit the spec body + History to state AS-SHIPPED vs intent, record deferred future-fixes with the concrete one-line fix, and flip #+state ready->complete only once the UI is integrated+verified.","FROZEN-CLOCK RENDER RECIPE STILL HOLDS: make deps installs org-ql etc. into repo-local .deps/; render with current-time cl-letf’d to org-air-test-now (Mon 2026-06-15 10:00) inside org-air-test-with-fixtures; load tests/org-air-test-helpers.el (test-init alone is not enough — with-fixtures lives in helpers). Dump buffer-substring + face runs at all rail width tiers (28/32/42 ~ mockups 96/120/160). jj sandbox: GIT_CONFIG_GLOBAL=/dev/null + --config signing.behavior=drop, commit <paths> early/often, verify non-empty with jj diff -r @- --stat."]}
