[org-air3.work.test.learnings:36984] {"agent":"org-air-test","learnings":["RESTART RECOVERY WORKED: re-creating grinds from the SURVIVED design specs (ymroopnp/utwrpzmx) + committing early/often (every milestone, jj commit not bare describe, verify non-empty with jj diff -r @- --stat) fully recovered the lost round-9 test track. The known-failures manifest made each impl milestone a one-line diff (grind flips green -> delete entry); round closed with manifest EMPTY.","NEW TRAP — rebase relocates a change-id, silently re-pairing code with stale fixtures: I rebased my whole test chain forward (-s mktuwtzz -d rskvknsz) across successive impl tips. That carried an EARLIER blessed commit (vxmtqvqm) onto a LATER impl commit (rskvknsz), so vxmtqvqm then shipped rskvknsz flush-col-0 view.el WITH its own pre-flush blessed fixtures = internally mismatched. The orchestrator merge(qqpuoqlv, vxmtqvqm) then failed 4 mockups and looked like a render mismatch but was a rebase artifact. LESSON: when a named commit is the integration anchor, do NOT rebase it; verify its ancestry (jj log -r ::X & impl-tips) before merging, and build integration off the CODE tip + RESTORE the blessed fixture CONTENT (jj restore --from BLESSED tests/) rather than using a relocated change as a parent.","FIXTURE<->CODE PAIRING IS THE TEST ROLE DELIVERABLE: prove it explicitly every integration — (a) jj diff --from BLESSED --to TIP tests/fixtures = 0 (byte-identical -> no re-bless), (b) grep the rendered .el for the deferred feature marker to confirm the right code tip, (c) make clean before EVERY verify so no .elc shadows. A scoped-only feature (Q1 S reset) does NOT change the unfiltered canonical mockups, so qqpuoqlv fixtures == ysvynsyl fixtures — reason about which render conditions a fixture actually exercises before regenerating.","PROMPT-FORWARD: keep exact change-ids for every parent/anchor AND state which are frozen vs may-move; a settled tip that moves 4x in one round (ysvynsyl->qqpuoqlv->rskvknsz, plus a deferred branch) needs the orchestrator to name the ONE frozen integration anchor and the deferred ones explicitly, which it eventually did (integrate qqpuoqlv lineage, defer rskvknsz)."]}
