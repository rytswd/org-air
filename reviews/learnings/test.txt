[org-air.work.test.learnings:36101] {"agent":"org-air-test","learnings":["SANDBOX/JJ SURVIVAL KIT (use verbatim): every jj call needs GIT_CONFIG_GLOBAL=/dev/null and --config signing.behavior=drop (gpg + yubikey config are sandbox-denied; human signs at integration). rm -f *.elc tests/*.elc before EVERY verification - stale byte-code silently shadows amended sources and reproduces fixed bugs. jj restore deletes files that exist only in the working copy - back up uncommitted new files before any restore. update-stale after a sibling amend creates divergent change ids and drops untracked files; put reports in commits or /tmp, never loose in the workspace.","SELF-POLICING BASELINES ARE THE BEST CONVENTION WE FOUND: tests/org-air-known-failures.el (expected-fail manifest) and tests/org-air-lint-baseline.el (lint allowlist) both use the same contract - listed-and-failing=green, listed-and-passing/fixed=RED until the entry is deleted. This made make check a single binary exit code for orchestration AND turned every closeout into a one-line diff. It worked end to end: the manifest flagged the sign-fix landing automatically, and impl burned the entire 13-entry lint baseline to zero. Keep this pattern; never let a suite go red-but-expected by convention alone.","SPEC-TRUE ASSERTIONS, IMPL-TRUE NEVER: every test that encoded a current-impl detail (face name org-air-face-popout, banner in buffer text) broke when impl converged on air/v0.1/org-air-design.org (org-air-face-calendar-today, header-line-format). Write assertions from the design doc frozen names from day one; treat the spec as the only stable interface. Corollary: grind tests (spec-true tests the impl fails) were the highest-leverage artifact in the project - 7 in round 2, 5 in round 3, all fixed within one round each because the failure messages were precise punch lists.","BATCH EMACS TRAPS (cost us real cycles): format-mode-line returns empty in --batch (walk the mode-line construct strings instead); org-done-keywords is buffer-local and globally nil (classification outside org buffers needs explicit handling - this leaked DONE items); org-get-heading t strips [#A] before org-get-priority can parse it; org-get-tags LOCAL=t hides #+filetags; raw time-subtract floor() is not calendar-day math (use time-to-days) - a today-deadline lands in NO bucket otherwise; functions reading (current-time) internally need cl-letf freezing in tests, and fixture determinism needs one frozen now constant (org-air-test-now) that every suite shares.","PROMPT IMPROVEMENTS FOR NEXT ITERATION: keep exact change-ids for merge parents and exact ever topic names in every directive (zero ambiguity rounds were the fastest); pre-create topics or grant creation explicitly; state the .gitignore/conflict-resolution policy and signing policy upfront; require status events IMMEDIATELY after trunk verification and before new work (cheap, kept orchestrator unblocked); and keep fixture edits in the test-track scope - adding one fixture heading requires auditing count-asserting harness tests (we hit 9-vs-10 TODO count exactly once and only because the harness pinned it)."]}
[org-air.work.org-air-test.learnings:35959] {"agent":"org-air-test","learnings":["Sandbox: jj commit needs both ~/.config/git/yubikey-status (git config include) and ~/.gnupg (signing.behavior=own + gpg). Workaround that worked: GIT_CONFIG_GLOBAL=/dev/null jj --config signing.behavior=drop per invocation. Next round prompt should state the signing policy upfront (commit unsigned vs wait) to avoid a blocked round-trip.","Event topics in the prompt (org-air.work.org-air-test.*) did not exist; ever pub fails with topic-not-found and pre-existing topics used a different scheme (org-air.work.test.*). Either pre-create topics or tell workers they may create them.","Stale .elc artifacts silently shadow amended .el sources (Emacs prefers older byte-compiled files, only emitting a warning), which made a fixed impl reproduce old failures. Next prompt: always rm -f *.elc (or add a make clean dependency) before verifying after a parent rewrite.","jj workspace staleness after an in-place amend of a sibling produces divergent change ids and silently drops untracked working-copy files (my reviews/verify-round2.md vanished on update-stale). Put verification reports in a committed change or outside the workspace, and expect to abandon divergent merges.","Verification was much faster by root-causing before reporting: 18 of 20 failures collapsed into one defect (entry point never loaded subsystems) while the genuinely independent bugs (filetags vs local tags, priority cookie stripping, non-calendar day math) needed targeted batch-eval probes. Next prompt: keep the watch-list hints — they pointed exactly at the real defects."]}
[org-air.work.org-air-test.learnings:36052] {"agent":"org-air-test","learnings":["Round-1 learnings held up well: the jj sandbox flags, topic pre-creation, and rm -f *.elc guidance were all baked into the round-2 prompt and saved real time; zero blocked round-trips this round. Keep carrying them forward verbatim.","New trap: batch Emacs UI blind spots. format-mode-line returns empty string in --batch (no window), so header-line/mode-line assertions must walk the construct strings directly. Similar class: faces, display properties and keymaps work in batch but anything needing redisplay does not. Round-3 prompt should note which UI surfaces are batch-testable.","New trap: tests written against the current impl rot when impl converges on the spec. My calendar-today and banner tests encoded round-1 impl details (org-air-face-popout, banner in buffer text) and broke when impl correctly adopted spec faces and header-line-format. Write assertions against spec-frozen names/locations from the start, and treat any impl-shaped assertion as tech debt.","Expected-failure tests as grind signals worked: 7 deliberately failing spec tests gave impl a precise punch list and 5 were fixed in one round, the rest in closeout. But ERT lacks expected-failure bookkeeping, so unexpected counts needed manual interpretation in every report. Round 3: consider :expected-result :failed or a known-failures manifest so make test is binary green/red for orchestration.","Verification merges are cheap and high-signal in jj: a throwaway undescribed multi-parent working copy (rm elc first, leave undescribed, never describe/commit it) verified every sibling combination this round, including a mid-task parent swap (yslwxrmy -> vvttnzqq) with zero friction. Round-3 prompt should keep specifying exact change ids for merge parents - it removed all ambiguity."]}
