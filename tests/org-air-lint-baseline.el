;;; org-air-lint-baseline.el --- accepted lint findings -*- lexical-binding: t; -*-

;;; Commentary:
;; Baseline of accepted lint findings.  This list is intentionally empty:
;; lint issues must be fixed rather than accepted.

;;; Code:

(defconst org-air-lint-baseline nil
  "Accepted lint findings; must remain empty.")

(provide 'org-air-lint-baseline)
;;; org-air-lint-baseline.el ends here
